/* Generated by scripts/generate-service-worker.mjs. */
const CACHE_PREFIX = "mabis-offline-";
const SHELL_CACHE = CACHE_PREFIX + "shell-5e102903655a";
const RUNTIME_CACHE = CACHE_PREFIX + "runtime-5e102903655a";
const LAYOUT_CACHE = CACHE_PREFIX + "layout-5e102903655a";
const PRECACHE_URLS = [
  "/index.html",
  "/manifest.json",
  "/fonts/gnu-freefont/FreeMono.woff2?v=2",
  "/fonts/gnu-freefont/FreeMonoBold.woff2?v=2",
  "/assets/index-CqF1SMRA.js",
  "/assets/index-DNK3gCzI.css",
  "/assets/Home-Dn1pATuD.js",
  "/assets/format-i5OvjDgx.js",
  "/assets/Icon-BLqOq7Ax.js",
  "/assets/x-Dew4lBWj.js",
  "/assets/chevron-down-C1cZxI0m.js",
  "/assets/usePresence-BvDmIf2t.js",
  "/assets/themes-eWx4sLJt.js",
  "/assets/pride-Jwj8n100.js",
  "/assets/palette-D5c20urz.js",
  "/assets/check-BNQbXbhn.js",
  "/assets/save-B-O0qyMd.js",
  "/assets/star-seRXz24l.js",
  "/assets/trash-2-CTAEI6BR.js",
  "/assets/IdleMount-C0Z2kIzJ.js",
  "/assets/SettingsModal-DuMHeDK_.js",
  "/assets/font-catalog-BvCrnvDm.js",
  "/assets/dialog-BK1tJK6T.js",
  "/assets/index-CC6hX-s-.js",
  "/assets/search-Dim8SWCD.js",
  "/assets/MabisAIAssistant-BmNTpzMr.js",
  "/assets/geminiClient-BVqZFwhM.js",
  "/assets/weeks-D7NtaTEP.js",
  "/assets/isFriday-CngpKXdZ.js",
  "/assets/nextFriday-D07-J7ny.js",
  "/assets/maximize-2-CFsIJ81-.js",
  "/assets/minimize-2-CA4AXFpB.js",
  "/assets/send-BZwO8F9w.js",
  "/assets/FeedbackWidget-jFNPrITZ.js",
  "/assets/image-plus-C5k4OBMw.js",
  "/assets/loader-circle-jstQMgUW.js",
  "/assets/ProfileEditor-CfP2PZ2J.js",
  "/assets/rotate-ccw-clock-BtRNF7mU.js",
  "/assets/JobReminder-DHc0zUGW.js",
  "/assets/QuickStartGuide-cn3Obh4u.js",
  "/assets/LunchMenuWidget-BRc60u7d.js",
  "/assets/ScheduleWidget-BP-rCuvV.js",
  "/assets/ui-EADZ9u1h.js",
  "/assets/link-2-CF7tdl-j.js",
  "/assets/pencil-B64vbhnC.js",
  "/assets/JobsWidget-k7guP4sJ.js",
  "/assets/names-Be6cvtAd.js",
  "/assets/startOfMonth-BCuJEmTi.js",
  "/assets/user-plus-D4ntPJB9.js",
  "/assets/select-BZmRPJM5.js",
  "/assets/circle-check-BdrJZCBM.js",
  "/assets/plus-CRQjp2j3.js",
  "/assets/DiscussionWidget-CLxrySMP.js",
  "/assets/chevron-left-CAxha1tG.js",
  "/assets/chevron-right-BRjPh57x.js",
  "/assets/pause-Dw0LL681.js",
  "/assets/CalendarWidget-DbjNp6Bd.js",
  "/assets/AnnouncementsWidget-BeJByQZq.js",
  "/assets/image-CyGJ6LsG.js",
  "/assets/video-D3NAazRq.js",
  "/assets/NewsWidget-BcSjP4_z.js",
  "/assets/MeetingModeWidget-CLiSUwxK.js",
  "/assets/undo-2-CILqRY5x.js",
  "/assets/MissingItemsWidget-DuKxT8EZ.js",
  "/assets/MembersWidget-CX9dHbyQ.js",
  "/assets/users-CUPSorE6.js",
  "/assets/offline-cache-BozEg46k.js"
];
/* Assets only the boss Home layout executes. Not in the install precache: the
   default layout would pay to download and store code it never runs. */
const BOSS_LAYOUT_URLS = [
  "/assets/boss-DY-ew7xu.js",
  "/assets/offline-cache-BozEg46k.js",
  "/assets/DocsEditor-nfUJ8R_p.js",
  "/assets/DocsEditor-CTpA5PSA.css",
  "/assets/underline-2tLbAoSP.js",
  "/assets/MeetingMinutes-ByL4mEO9.js",
  "/assets/MeetingNotesEditor-CuYDQCaw.js",
  "/assets/PasswordModal-peHCquWh.js",
  "/assets/home-warmup-BISwx7WT.js",
  "/assets/Splash-CjDuZ-Pb.js",
  "/assets/arrow-up-right-cMR_ylAP.js",
  "/assets/Login-uR1iK4yx.js",
  "/assets/History-uPTxCu_9.js",
  "/assets/page-chrome-DE_-pCg_.js",
  "/assets/AnnouncementsHistory-D43qsbmX.js",
  "/assets/ArchivePage-Bu8OVqld.js",
  "/assets/NewsHistory-C0LGyaG1.js",
  "/assets/Feedback-CLDwt66v.js",
  "/assets/AnalyticsTab-CpQTwYYR.js",
  "/assets/CustomCursor-BDuZN1gS.js",
  "/assets/prefs_sync-D0Axl1RL.js",
  "/assets/NotFound-B71_wXhF.js",
  "/assets/ScrollVelocity-CiPGLn6I.js",
  "/assets/ScrollScaleRitual-D1-OLgEn.js"
];
const MAX_RUNTIME_ENTRIES = 48;

self.addEventListener("install", (event) => {
  event.waitUntil(caches.open(SHELL_CACHE).then((cache) => cache.addAll(PRECACHE_URLS)));
});

self.addEventListener("activate", (event) => {
  event.waitUntil((async () => {
    const names = await caches.keys();
    await Promise.all(
      names
        .filter((name) => name.startsWith(CACHE_PREFIX)
          && name !== SHELL_CACHE && name !== RUNTIME_CACHE && name !== LAYOUT_CACHE)
        .map((name) => caches.delete(name)),
    );
    if (self.registration.navigationPreload) {
      await self.registration.navigationPreload.enable();
    }
    await self.clients.claim();
  })());
});

/*
 * Cache exactly one layout's assets: whichever one the reader is using.
 *
 * The page posts this on boot and on every change, so switching to the boss
 * layout fetches its chunks into the offline shell, and switching back deletes
 * them rather than leaving a layout in storage that is no longer rendered.
 */
async function applyLayout(layout) {
  const cache = await caches.open(LAYOUT_CACHE);
  if (layout === "boss") {
    await cache.addAll(BOSS_LAYOUT_URLS);
    return;
  }
  await Promise.all(BOSS_LAYOUT_URLS.map((url) => cache.delete(url)));
}

self.addEventListener("message", (event) => {
  if (event.data?.type !== "mabis-home-layout") return;
  // Never let a cache write fail the message: an offline switch just means the
  // chunks are fetched on demand the next time the layout renders.
  event.waitUntil(applyLayout(event.data.layout).catch(() => {}));
});

async function trim(cache, limit) {
  const keys = await cache.keys();
  const overflow = keys.length - limit;
  if (overflow > 0) {
    await Promise.all(keys.slice(0, overflow).map((request) => cache.delete(request)));
  }
}

async function appShellNavigation(event) {
  const shell = await caches.open(SHELL_CACHE);
  try {
    const preloaded = await event.preloadResponse;
    if (preloaded?.ok) {
      const type = preloaded.headers.get("content-type") || "";
      if (type.includes("text/html")) await shell.put("/index.html", preloaded.clone());
      return preloaded;
    }

    const response = await fetch(event.request);
    if (response.ok && (response.headers.get("content-type") || "").includes("text/html")) {
      await shell.put("/index.html", response.clone());
    }
    return response;
  } catch {
    return (await shell.match("/index.html")) || Response.error();
  }
}

async function staticAsset(request) {
  const cached = await caches.match(request, { ignoreSearch: true });
  if (cached) return cached;

  const response = await fetch(request);
  if (response.ok && response.type === "basic") {
    const runtime = await caches.open(RUNTIME_CACHE);
    await runtime.put(request, response.clone());
    await trim(runtime, MAX_RUNTIME_ENTRIES);
  }
  return response;
}

self.addEventListener("fetch", (event) => {
  const request = event.request;
  if (request.method !== "GET") return;

  const url = new URL(request.url);
  if (url.origin !== self.location.origin) return;

  // Authenticated Base44 data is deliberately never placed in Cache Storage.
  if (
    url.pathname.startsWith("/api/") ||
    url.pathname.startsWith("/functions/") ||
    request.headers.has("authorization")
  ) {
    return;
  }

  if (request.mode === "navigate") {
    event.respondWith(appShellNavigation(event));
    return;
  }

  if (
    url.pathname.startsWith("/assets/") ||
    url.pathname.startsWith("/fonts/") ||
    ["script", "style", "font", "image"].includes(request.destination)
  ) {
    event.respondWith(staticAsset(request));
  }
});
