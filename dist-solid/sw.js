/* Generated by scripts/generate-service-worker.mjs. */
const CACHE_PREFIX = "mabis-offline-";
const SHELL_CACHE = CACHE_PREFIX + "shell-ff7014637b4d";
const RUNTIME_CACHE = CACHE_PREFIX + "runtime-ff7014637b4d";
const LAYOUT_CACHE = CACHE_PREFIX + "layout-ff7014637b4d";
const PRECACHE_URLS = [
  "/index.html",
  "/manifest.json",
  "/fonts/gnu-freefont/FreeMono-subset.woff2?v=3",
  "/fonts/gnu-freefont/FreeMonoBold-subset.woff2?v=3",
  "/assets/index-BTO6wxPg.js",
  "/assets/index-C-aAxXpr.css",
  "/assets/rolldown-runtime-hePW80VL.js",
  "/assets/web-CRd2b-C6.js",
  "/assets/routing-DMQjvnPS.js",
  "/assets/build-DKxEHsf3.js",
  "/assets/hydration-DLvSR0CP.js",
  "/assets/performance-tier-Di7Gdr5E.js",
  "/assets/refresh-rate-B0YSulQf.js",
  "/assets/query-client-BjYC8k6T.js",
  "/assets/base44Client-Qava90MS.js",
  "/assets/preload-helper-Czpn1I53.js",
  "/assets/hacker-CpiXj06y.js",
  "/assets/math-DNTvH3Wz.js",
  "/assets/japanese-text-preference-wUbVEDmC.js",
  "/assets/scroll-progress-BJCDJTnX.js",
  "/assets/tokens-Bsoxdnlc.js",
  "/assets/Home-FiwXP2h5.js",
  "/assets/Icon-DovX7xqt.js",
  "/assets/BirthdayBanner-CPEUT2S7.js",
  "/assets/x-fCeCNi14.js",
  "/assets/format-BYnr62Nj.js",
  "/assets/check-qU1aE4FR.js",
  "/assets/chevron-down-D3RCbcyr.js",
  "/assets/settings-BwwHMSnL.js",
  "/assets/palette-DqBATHEV.js",
  "/assets/save-BLAxa5Jd.js",
  "/assets/search-CSeFw0Ac.js",
  "/assets/star-BfSr0psW.js",
  "/assets/trash-2-rB5znZ7_.js",
  "/assets/IdleMount-Bu-NW3je.js",
  "/assets/prefs-D1rauskJ.js",
  "/assets/usePresence-BL14k3If.js",
  "/assets/themes-VQQooITF.js",
  "/assets/pride-7X_H22h-.js",
  "/assets/SettingsModal-Dmrc-Kkz.js",
  "/assets/dialog-BKgm69_k.js",
  "/assets/dist-B2s6xB6g.js",
  "/assets/font-catalog-Di6S8hdR.js",
  "/assets/MabisAIAssistant-C32aQ-eu.js",
  "/assets/maximize-2-7eiwKvka.js",
  "/assets/minimize-2-S4f1TQOo.js",
  "/assets/send-DbCyfOjP.js",
  "/assets/geminiClient-DZ5S_b7g.js",
  "/assets/weeks-D51O-gnV.js",
  "/assets/nextFriday-0S6QM2AP.js",
  "/assets/isFriday-DrR5X27V.js",
  "/assets/FeedbackWidget-CIpfWbA0.js",
  "/assets/image-plus-DinakDtZ.js",
  "/assets/loader-circle-DIwPz9aa.js",
  "/assets/ProfileEditor-8rp8CAa3.js",
  "/assets/rotate-ccw-clock-B6sgnkMZ.js",
  "/assets/JobReminder-DTFQ7J1K.js",
  "/assets/QuickStartGuide-BLLwkiet.js",
  "/assets/LunchMenuWidget-CSvkONgU.js",
  "/assets/ScheduleWidget-B9Bv-R4X.js",
  "/assets/link-2-DbxWIXz3.js",
  "/assets/pencil-Dw1rtfzN.js",
  "/assets/ui-BUVK7uGz.js",
  "/assets/JobsWidget-D-qH0orp.js",
  "/assets/circle-check-7D-S0WBN.js",
  "/assets/plus-ud6biY9l.js",
  "/assets/addWeeks-DuOb_WLf.js",
  "/assets/startOfMonth-DqZh9M2d.js",
  "/assets/names-CVL1ZItE.js",
  "/assets/select-Ci_I9CNq.js",
  "/assets/DiscussionWidget-CjX31DGU.js",
  "/assets/chevron-left-DZnWUwGl.js",
  "/assets/chevron-right-CY6hCegy.js",
  "/assets/pause-DkZHcrzb.js",
  "/assets/CalendarWidget-z-6HEIJd.js",
  "/assets/AnnouncementsWidget-B2NVU5DF.js",
  "/assets/image-CShYTTZv.js",
  "/assets/video-DPPOfuxY.js",
  "/assets/NewsWidget-ChAMjdre.js",
  "/assets/MeetingModeWidget-Dp0N_dUf.js",
  "/assets/arrow-right-C2S7Ftt_.js",
  "/assets/undo-2-n9SF8oDK.js",
  "/assets/MissingItemsWidget-BVFUS9Ll.js",
  "/assets/MembersWidget-BTmem3gs.js",
  "/assets/users-DzchERCB.js",
  "/assets/offline-cache-2XVD210L.js"
];
/* Assets only the boss Home layout executes. Not in the install precache: the
   default layout would pay to download and store code it never runs. */
const BOSS_LAYOUT_URLS = [
  "/assets/boss-Be8hOOGv.js",
  "/assets/boss-O6UnAcPx.css",
  "/assets/page-chrome-Do4RoeAo.js",
  "/assets/arrow-up-right-B9M0KhYf.js",
  "/assets/ScrollVelocity-Ces3rhDj.js",
  "/assets/ScrollScaleRitual-D2Pkrene.js"
];
const MAX_RUNTIME_ENTRIES = 48;

self.addEventListener("install", (event) => {
  /*
   * Take over immediately instead of waiting for every tab to close.
   *
   * Without this, a new build installs and then sits in "waiting" for as long
   * as ANY tab of the app is open — and because staticAsset() is cache-first,
   * the old worker keeps serving the old JS and CSS from its shell cache the
   * whole time. Even a hard reload gets the stale build. On a PWA that people
   * leave open on a phone, "as long as any tab is open" is close to forever:
   * fixes were shipping, building green, and never reaching anyone.
   *
   * The trade-off is the usual one — a page loaded from the previous build
   * may afterwards ask for a chunk this build has renamed. The client handles
   * that by reloading once on controllerchange (see solid/main.jsx), which is
   * a single refresh against a fix that otherwise never arrives.
   */
  self.skipWaiting();
  event.waitUntil(caches.open(SHELL_CACHE).then((cache) => cache.addAll(PRECACHE_URLS)));
});

self.addEventListener("activate", (event) => {
  event.waitUntil((async () => {
    const names = await caches.keys();
    await Promise.all(
      names
        .filter((name) => name.startsWith(CACHE_PREFIX)
          && name !== SHELL_CACHE && name !== RUNTIME_CACHE && name !== LAYOUT_CACHE)
        .map((name) => caches.delete(name)),
    );
    if (self.registration.navigationPreload) {
      await self.registration.navigationPreload.enable();
    }
    await self.clients.claim();
  })());
});

/*
 * Cache exactly one layout's assets: whichever one the reader is using.
 *
 * The page posts this on boot and on every change, so switching to the boss
 * layout fetches its chunks into the offline shell, and switching back deletes
 * them rather than leaving a layout in storage that is no longer rendered.
 */
async function applyLayout(layout) {
  const cache = await caches.open(LAYOUT_CACHE);
  if (layout === "boss") {
    await cache.addAll(BOSS_LAYOUT_URLS);
    return;
  }
  await Promise.all(BOSS_LAYOUT_URLS.map((url) => cache.delete(url)));
}

self.addEventListener("message", (event) => {
  if (event.data?.type !== "mabis-home-layout") return;
  // Never let a cache write fail the message: an offline switch just means the
  // chunks are fetched on demand the next time the layout renders.
  event.waitUntil(applyLayout(event.data.layout).catch(() => {}));
});

async function trim(cache, limit) {
  const keys = await cache.keys();
  const overflow = keys.length - limit;
  if (overflow > 0) {
    await Promise.all(keys.slice(0, overflow).map((request) => cache.delete(request)));
  }
}

async function appShellNavigation(event) {
  const shell = await caches.open(SHELL_CACHE);
  try {
    const preloaded = await event.preloadResponse;
    if (preloaded?.ok) {
      const type = preloaded.headers.get("content-type") || "";
      if (type.includes("text/html")) await shell.put("/index.html", preloaded.clone());
      return preloaded;
    }

    const response = await fetch(event.request);
    if (response.ok && (response.headers.get("content-type") || "").includes("text/html")) {
      await shell.put("/index.html", response.clone());
    }
    return response;
  } catch {
    return (await shell.match("/index.html")) || Response.error();
  }
}

async function staticAsset(request) {
  const cached = await caches.match(request, { ignoreSearch: true });
  if (cached) return cached;

  const response = await fetch(request);
  if (response.ok && response.type === "basic") {
    const runtime = await caches.open(RUNTIME_CACHE);
    await runtime.put(request, response.clone());
    await trim(runtime, MAX_RUNTIME_ENTRIES);
  }
  return response;
}

self.addEventListener("fetch", (event) => {
  const request = event.request;
  if (request.method !== "GET") return;

  const url = new URL(request.url);
  if (url.origin !== self.location.origin) return;

  // Authenticated Base44 data is deliberately never placed in Cache Storage.
  if (
    url.pathname.startsWith("/api/") ||
    url.pathname.startsWith("/functions/") ||
    request.headers.has("authorization")
  ) {
    return;
  }

  if (request.mode === "navigate") {
    event.respondWith(appShellNavigation(event));
    return;
  }

  if (
    url.pathname.startsWith("/assets/") ||
    url.pathname.startsWith("/fonts/") ||
    ["script", "style", "font", "image"].includes(request.destination)
  ) {
    event.respondWith(staticAsset(request));
  }
});
