/* Generated by scripts/generate-service-worker.mjs. */
const CACHE_PREFIX = "mabis-offline-";
const SHELL_CACHE = CACHE_PREFIX + "shell-f78fe7c95d9d";
const RUNTIME_CACHE = CACHE_PREFIX + "runtime-f78fe7c95d9d";
const PRECACHE_URLS = [
  "/index.html",
  "/manifest.json",
  "/fonts/gnu-freefont/FreeMono.woff2?v=2",
  "/fonts/gnu-freefont/FreeMonoBold.woff2?v=2",
  "/assets/index-oEDR9RmT.js",
  "/assets/index-DZYYyEAE.css",
  "/assets/Home-BgH-zWGd.js",
  "/assets/usePresence-CUEeYqBR.js",
  "/assets/arrow-up-right-CJBeZzDk.js",
  "/assets/Icon-CJ2oktMZ.js",
  "/assets/themes-w4jtBTPg.js",
  "/assets/pride-Jwj8n100.js",
  "/assets/palette-CzBlRoS0.js",
  "/assets/check-BXx8ha-r.js",
  "/assets/save-Cifh8Azg.js",
  "/assets/star-BJLUx6Yw.js",
  "/assets/trash-2-CUEaviB0.js",
  "/assets/IdleMount-B2y-WWYr.js",
  "/assets/format-i5OvjDgx.js",
  "/assets/x-Bl_I6D37.js",
  "/assets/page-chrome-D14OdfHL.js",
  "/assets/plus-CQhvhLkI.js",
  "/assets/chevron-down-CoGzOuM-.js",
  "/assets/prefs-OfvX8uFe.js",
  "/assets/SettingsModal-dPfV6hCi.js",
  "/assets/font-catalog-BvCrnvDm.js",
  "/assets/dialog-Do96kwem.js",
  "/assets/index-TTnLJy4M.js",
  "/assets/search-DbnsjVD_.js",
  "/assets/MabisAIAssistant-CI4qOJ8E.js",
  "/assets/geminiClient-BVqZFwhM.js",
  "/assets/weeks-B7cnPmm4.js",
  "/assets/isFriday-CngpKXdZ.js",
  "/assets/nextFriday-D07-J7ny.js",
  "/assets/maximize-2-COaP36wd.js",
  "/assets/minimize-2-DfyFfTaK.js",
  "/assets/send-BZeCy3Q7.js",
  "/assets/FeedbackWidget-CVcTOuX5.js",
  "/assets/image-plus-CQrD-Lj-.js",
  "/assets/loader-circle-yOwXv_B6.js",
  "/assets/ProfileEditor-BbNuZx_V.js",
  "/assets/rotate-ccw-clock-CVFZ007B.js",
  "/assets/JobReminder-e6-kdJ5t.js",
  "/assets/QuickStartGuide-D8qcAp8e.js",
  "/assets/LunchMenuWidget-BrnsY7XM.js",
  "/assets/ScheduleWidget-l7wIBZZp.js",
  "/assets/ui-x8dPOpbw.js",
  "/assets/link-2-CNM0-n8v.js",
  "/assets/pencil-RxeLSrat.js",
  "/assets/JobsWidget-Dq-brS5z.js",
  "/assets/names-Be6cvtAd.js",
  "/assets/startOfMonth-BCuJEmTi.js",
  "/assets/user-plus-CG8nNz6u.js",
  "/assets/select-D-cbiC8O.js",
  "/assets/circle-check-DJadZ3fb.js",
  "/assets/DiscussionWidget-BRCy86mz.js",
  "/assets/chevron-left-frNTxQRl.js",
  "/assets/chevron-right-D2H9B78z.js",
  "/assets/pause-C6HGTJsg.js",
  "/assets/CalendarWidget-W_ALcmpD.js",
  "/assets/AnnouncementsWidget-DY-YU7Sf.js",
  "/assets/image-CB3Vqlvz.js",
  "/assets/video-DJ3-HhR-.js",
  "/assets/NewsWidget-Bw3OThc9.js",
  "/assets/MeetingModeWidget-IcuzmPd_.js",
  "/assets/undo-2-D1bkTkBc.js",
  "/assets/MissingItemsWidget-Df7RyMqn.js",
  "/assets/MembersWidget-Dbed1DOz.js",
  "/assets/users-DWJsgfBS.js",
  "/assets/offline-cache-DW0-Y8nL.js"
];
const MAX_RUNTIME_ENTRIES = 48;

self.addEventListener("install", (event) => {
  event.waitUntil(caches.open(SHELL_CACHE).then((cache) => cache.addAll(PRECACHE_URLS)));
});

self.addEventListener("activate", (event) => {
  event.waitUntil((async () => {
    const names = await caches.keys();
    await Promise.all(
      names
        .filter((name) => name.startsWith(CACHE_PREFIX) && name !== SHELL_CACHE && name !== RUNTIME_CACHE)
        .map((name) => caches.delete(name)),
    );
    if (self.registration.navigationPreload) {
      await self.registration.navigationPreload.enable();
    }
    await self.clients.claim();
  })());
});

async function trim(cache, limit) {
  const keys = await cache.keys();
  const overflow = keys.length - limit;
  if (overflow > 0) {
    await Promise.all(keys.slice(0, overflow).map((request) => cache.delete(request)));
  }
}

async function appShellNavigation(event) {
  const shell = await caches.open(SHELL_CACHE);
  try {
    const preloaded = await event.preloadResponse;
    if (preloaded?.ok) {
      const type = preloaded.headers.get("content-type") || "";
      if (type.includes("text/html")) await shell.put("/index.html", preloaded.clone());
      return preloaded;
    }

    const response = await fetch(event.request);
    if (response.ok && (response.headers.get("content-type") || "").includes("text/html")) {
      await shell.put("/index.html", response.clone());
    }
    return response;
  } catch {
    return (await shell.match("/index.html")) || Response.error();
  }
}

async function staticAsset(request) {
  const cached = await caches.match(request, { ignoreSearch: true });
  if (cached) return cached;

  const response = await fetch(request);
  if (response.ok && response.type === "basic") {
    const runtime = await caches.open(RUNTIME_CACHE);
    await runtime.put(request, response.clone());
    await trim(runtime, MAX_RUNTIME_ENTRIES);
  }
  return response;
}

self.addEventListener("fetch", (event) => {
  const request = event.request;
  if (request.method !== "GET") return;

  const url = new URL(request.url);
  if (url.origin !== self.location.origin) return;

  // Authenticated Base44 data is deliberately never placed in Cache Storage.
  if (
    url.pathname.startsWith("/api/") ||
    url.pathname.startsWith("/functions/") ||
    request.headers.has("authorization")
  ) {
    return;
  }

  if (request.mode === "navigate") {
    event.respondWith(appShellNavigation(event));
    return;
  }

  if (
    url.pathname.startsWith("/assets/") ||
    url.pathname.startsWith("/fonts/") ||
    ["script", "style", "font", "image"].includes(request.destination)
  ) {
    event.respondWith(staticAsset(request));
  }
});
